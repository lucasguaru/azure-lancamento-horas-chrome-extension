{
  "name": "chrome-ado-hours",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "predev": "node scripts/sync-manifest-version.mjs",
    "prebuild": "node scripts/sync-manifest-version.mjs",
    "build": "vite build",
    "prezip": "npm run build",
    "zip": "node scripts/zip-dist.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.315",
    "bestzip": "^2.2.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
