# Docs

Esta pasta guarda documentação e assets auxiliares do projeto.

## Imagens (screenshots)

Coloque os prints em `docs/images/` seguindo exatamente os nomes abaixo (o `README.md` da raiz já referencia esses arquivos).

- `01-chrome-extensions-developer-mode.png` — tela `chrome://extensions` destacando onde ativar **Developer mode**
- `02-chrome-extensions-load-unpacked.png` — tela `chrome://extensions` destacando o botão **Load unpacked**
- `03-chrome-extensions-import-zip-extract.png` — tela/fluxo do ZIP extraído sendo selecionado no **Load unpacked** (mostrar a pasta contendo `manifest.json` e `content.js`)

- `04-weekly-overlay-button.png` — botão flutuante 🕒 visível no Azure DevOps (canto inferior direito)
- `05-weekly-overlay-modal.png` — modal do overlay semanal aberto (visão geral)
- `06-weekly-overlay-edit-mode.png` — overlay semanal em modo edição (ex.: células alteradas / botões salvar/cancelar)
- `07-weekly-overlay-range-mode.png` — overlay semanal em modo “range” com inputs de início/fim preenchidos

- `08-monthly-hierarchy-button.png` — botão flutuante 📊 visível no Azure DevOps
- `09-monthly-hierarchy-modal.png` — modal do relatório mensal aberto
- `10-monthly-hierarchy-copy-image.png` — evidência do botão/ação “copiar imagem” (ex.: tooltip/mensagem de sucesso)

- `11-create-task-enhancer-inline.png` — UI inline do enhancer na tela de criação de task

- `12-autoparent-url-param.png` — screenshot mostrando a URL com `lh_parent=<id>`
- `13-autoparent-modal-add-link.png` — modal “Add link” com o parent selecionado/adicionado

## Dica de captura

- Use viewport de pelo menos **1280px** de largura para legibilidade.

